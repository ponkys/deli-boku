/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { BasicLocationMapStyleDirective } from './basic-location-map-style.directive';

describe('Directive: BasicLocationMapStyle', () => {
  it('should create an instance', () => {
    let directive = new BasicLocationMapStyleDirective();
    expect(directive).toBeTruthy();
  });
});
